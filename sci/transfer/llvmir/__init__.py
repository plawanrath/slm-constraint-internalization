"""Non-MLIR SSA transfer target: a minimal textual LLVM IR subset.

Grammars (C1 / C1+C2), a dominance-aware SSA scope validator (C3), an
`llvm-as` verifier, an `lli` gold-differential functional harness, and the
prompt builder / generator glue for the LLVM-Spec-60 test set.
"""

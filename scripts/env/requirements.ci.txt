# CI subset (Linux, no MLX). CI runs: pytest -m "not slow and not docker and not mlx and not ollama"
lark==1.3.1
numpy>=1.26.0,<3
scipy>=1.14.0
pandas>=2.2.0
matplotlib>=3.9.0
requests>=2.32.0
pytest>=8.3.0
pytest-xdist>=3.6.0
